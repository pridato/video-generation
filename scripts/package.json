{
  "name": "clips-metadata-processor",
  "version": "1.0.0",
  "type": "module",
  "description": "Procesador de metadatos de clips para Supabase",
  "main": "upload-clips.js",
  "scripts": {
    "generate-sql": "node generate_sql_inserts.js",
    "migrate": "node upload-clips.js",
    "migrate:dry": "node upload-clips.js --dry-run",
    "migrate:clean": "CONFIRM_CLEAN=true node upload-clips.js --clean",
    "test": "node upload-clips.js --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "Tu nombre",
  "license": "MIT"
}